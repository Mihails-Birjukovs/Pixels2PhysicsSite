import type { JSX } from "react";
import { useTitleHook } from "../../../hooks/useTitleHook";

export const Publications = ():JSX.Element => {
    useTitleHook("Pixels2Phisics - Publications");

    return <>
        Publications.
    </>
}
