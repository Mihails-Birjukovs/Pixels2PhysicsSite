import type { JSX } from "react";
import { useTitleHook } from "../../../hooks/useTitleHook";

export const Contacts = ():JSX.Element => {
    useTitleHook("Pixels2Phisics - Contacts");

    return <>
        Contacts.
    </>
}